import styled from "@emotion/styled";

export const Container = styled.div`
  height: 36px;
  z-index: 110;
  position: fixed;
  bottom: 18px;
  right: 12px;
`;

export const BadgeRow = styled.div`
  display: flex;
  justify-content: space-evenly;
  align-items: center;
  background-color: rbga(0, 0, 0, 0.98);
`;
